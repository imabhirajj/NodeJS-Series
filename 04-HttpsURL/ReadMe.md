# Node.js HTTP Module – Basics

This project covers the fundamentals of the built-in `http` module in Node.js.
It explains how an HTTP server works, how requests are handled, and which
important methods and properties are used.

---

## 📌 What is the `http` module?

- `http` is a **core module** of Node.js
- It is used to **create web servers**
- It handles **request (req)** and **response (res)** objects
- No external package is required

---



